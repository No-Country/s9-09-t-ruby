<div class="container">
  <div class="header pt-4">
    <h1>Editar receta</h1>
  </div>

  <%= turbo_frame_tag "remote_modal" do %>
    <%= render "layouts/modal_content", title: "Editar receta" do %>
      <%= render "form", recipe: @recipe %>
    <% end %>
  <% end %>
</div>
